nome = (input ("insira um nome "))
idade = int (input ("insira idade( anos completos: "))

#repete quando a idade for valida 
while idade > 120 or idade < 0:
   print = int(input("idade(anos completos -ate 120 ano):"))
dias_vidas = idade * 365
print (f"{nome}, voce viveu {dias_vidas}")
